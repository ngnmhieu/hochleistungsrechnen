1.
Mehrere Nutzer können auf dem gleichen System Jobs laufen lassen, in dem man Jobs bzw. Aufträge an dem System übergibt.

2.
Das Batch-Queuing-System erstellt eine Queue in der,
die Jobs auf die verfügbaren Ressourcen verteilt und priorisiert werden.

3.
SLURM, Grid MP, Moab Cluster Suite.

4.
Simple Linux Utility for Resource Management (SLURM).

5.
`sbatch` Übergibt ein Batch-Skript an SLURM. Die Nutzung des Befehls beschreiben wir hier kurz.  

Man kann  dem Befehl ein Batch-Skript füttern entweder mit einer Datei oder per Standardeingabe: 
```
  $ sbatch mein-skript.sh
  salloc: Granted job allocation 65537

  $ sbatch <<EOF
  > #!/bin/sh
  > #SBATCH --partition=west
  > echo "Hochleistungsrechnen"
  > EOF
  sbatch: Submitted batch job 65541
```
Man muss bei der Übergabe des Auftrages einige Informationen angeben, wie welche Partition, Ausgabe Log-Datei, Fehler Log-Datei, ... Das lässt sich
entweder per Komando-Zeile oder in der Skript-Datei (mit #SBATCH ...) angeben.

6.
Mit dem Befehl "squeue".

7.
sview ist im vergleich mit squeue übersichtlicher.
Es wird eine GUI geöffnet, die aktuell laufende Jobs anzeigt und einen Überblick
über das Cluster gibt. Darüber hinaus lässt sich auf andere Informationen schnell zugreifen
und zwischen unterschiedlichen Sichten wechseln.

8.
Ja und zwar mit dem Befehl "scancel". 
Außerdem kann man auch mit Rechtsclick Cancel einen Job beenden.

9.
Ja.

10.
In sview mit Rechtsclick Full Info.
Oder mit den Komandozeilen `scontrol show jobid -dd <jobid>`

11.
Es gibt zwei mögliche Scheduling-Verfahren: 'sched/builtin' und 'sched/backfill'. 'sched/backfill' wird auf dem System verwendet
(in sview Options -> Config Info).

Erläuterung (https://slurm.schedmd.com/sched_config.html):

Backfill Scheduling schaut sich alle Jobs an (laufenden und ausstehenden). Die ausstehenden Jobs
werden nach Priorität sortiert. Wenn ein ausstehender Job die beabsichtliche Startzeit anderer
höher priorisierten Jobs beeinträchtigen, dann wird er ausgeführt.

Bei Builtin Scheduling werden alle Jobs strikt nach Priorität ausgeführt, was die Ausnutzung des System beeinflussen.


12.
$ salloc -p west -w west3
salloc: Granted job allocation 35019
$ ssh west3
$ hostname
west3

13.
ToDo:
-----------------------------------------------------------


14.
In sview kann man die Priorität sowohl nachsehen als auch anpassen.

15.
Partitionen west, abu, amd, magny, nehalem. 
Man kann eine Partition mithilfe des "-p"-Switches wählen.

Aufgabe 2.
Frage:
ToDo

Frage:
ToDo
